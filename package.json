{
  "name": "particles",
  "version": "0.0.1",
  "description": "Particle library with phisics methods",
  "main": "index.js",
  "scripts": {
    "build": "npm run javascript && npm run static",
    "javascript": "npx rollup -c",
    "static": "rsync -rm src/static dist"
  },
  "author": "Guillermo Soler Poquet",
  "license": "MIT",
  "dependencies": {
    "rimraf": "^2.6.3",
    "rollup": "^1.0.1",
    "rollup-plugin-replace": "^2.1.0"
  }
}
